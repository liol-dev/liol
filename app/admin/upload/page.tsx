// ============================================================
// ADMIN UPLOAD — /admin/upload (placeholder)
// Next build session: drag-and-drop multi-file zone, batch
// category selector, EXIF date-taken auto-fill (exifr), then
// push to ImageKit's Upload API + insert metadata rows into
// Supabase.
// ============================================================

export default function AdminUploadPage() {
  return (
    <>
      <h1 className="text-xl font-light tracking-wide">Upload</h1>
      <p className="mt-1 text-sm text-liol-subtext">
        Drag-and-drop uploading lands here next session.
      </p>
    </>
  );
}
